---
description: List current tasks (planned + in-progress)
---

Show the kanban view: list both `tasks/in-progress/` (current WIP) and `tasks/planned/` (queue).

Steps:
1. Run `ls tasks/in-progress/` and `ls tasks/planned/`.
2. Format the output as two sections with file counts. If a directory is empty, say "(empty)".
3. For each section, just list the slugs (filenames without `.md`) — one per line.
