---
description: Typecheck both workspaces (server + web)
---

Run `npm run typecheck` from the project root. It typechecks both workspaces in sequence.

Report any TypeScript errors. If both pass cleanly, just respond `✓ typecheck clean`.
